This RTAMonitor it's a simple text based server that prints to output the parameters and logs received from clients.

To build/install just run:

  $ make
  $ make install

To run you need to type:

  $ ./RTAMonitorQt --Ice.Config=config.monitor
